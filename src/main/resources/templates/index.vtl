<hr>
#if($sighting-reports)
  #parse($sighting-reports)
#end
#if($sightings)
  #parse($sightings)
#end
#if($animals)
  #parse($all-animals)
#end
#if($add-ranger)
  #parse($add-ranger)
#end
#if($all-rangers)
  #parse($all-rangers)
#end
#if($all-locations)
  #parse($all-locations)
#end
#if($add-animal)
  #parse($add-animal)
#end
#if($add-location)
  #parse($add-location)
#end
<hr>
#if($sightings)
  #parse($all-sightings)
#end
