<h1>$ranger.getName()</h1>
<h3>ID: $ranger.getId()</h3>

#if($ranger.getSightings().size() == 0)
  <h3>No Sightings Reports thus far.</h3>
#else
  #foreach($sighting in $ranger.getSightings())
    Location: $sighting.getLocation()
    Reported by: $sighting.getRangerName()
    ID: >$sighting.getId()
  #end
#end

#parse($sighting)
