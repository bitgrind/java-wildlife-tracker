<div class="col-md-6">
  <h3> Report Animal Sighting </h3>
  <form action="/sightings/$ranger.getId()" method="post">
    <div class="input-group">
      <div class="form-control">
        #if($animals.size() >= 0)
          <label>Select Animal</label>
          <select name="animalSelected">
            #foreach($animal in $animals)
              <option value="$animal.getId()">$animal.getName()</option>
            #end
          </select>
      </div>
      <div class="form-control">
        <label>Location</label>
        <input type="text" name="location">
      </div>

      <div class="form-control">
        <label>Your Name</label>
        <select name="rangerSelected"
          #foreach($ranger in $rangers)
            <option value="$ranger.getName()">$ranger.getName()</option>
          #end
        </select>
      </div>
        <button class="btn" type="submit">Submit Report</button>
      #end
    </div>
  </form>
</div>
